# =============================================================================
# BIDA ML Starter — Konfiguration
# =============================================================================
# Beim Projektstart nur das Nötigste ausfüllen.
# Alles andere ergänzen, wenn es gebraucht wird.
# =============================================================================

project:
  name: "Mein ML Projekt"
  version: "0.1.0"
  random_state: 42

snowflake:
  database: "ML_DB"
  schema: "DATA"
  warehouse: "ML_WH"
  results_schema: "INFERENCE"

# Tabelle ergänzen, sobald bekannt:
# tables:
#   training_data: "ML_DB.DATA.MEINE_TABELLE"
